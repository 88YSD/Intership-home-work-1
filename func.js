//1 TASK Write a funcion that accepts your firstName and lastName

  //const firstName = 'Yehor';
  //const lastName = 'Damchuk';
    //function sayWho (Yehor, Damchuk) {
        //return firstName + ' ' + lastName;} 
      //console.log(sayWho());

//2 TASK Write a function that accepts numbers and return their sum

   //function countSum(input){
             
    //if (toString.call(input) !== "[object Array]")
       //return false;
         
               //let total =  0;
               //for(let i=0;i<input.length;i++)
                 //{                  
                   //if(isNaN(input[i])){
                   //continue;
                    //}
                     //total += Number(input[i]);
                  //}
                //return total;
               //}
       //console.log(countSum([1,2,45]));
       //console.log(countSum([100,340,3]));
       //console.log(countSum([34,54,3]));

//3 TASK Write a function that count number of letters in provided string

   //function countLetters (str, letter) {

    //var letter_Count = 0;
    //for (var position = 0; position < str.length; position++) 
    //{
       //if (str.charAt(position) == letter) 
         //{
         //letter_Count += 1;
         //}
   //  }
    // return letter_Count;
  // };
   
        //console.log(countLetters('Node developer', 'd'));

//4 TASK Write function that will return random integer in range that you provided

  //function getRandom(min, max) {
   // min = Math.ceil(min);
   // max = Math.floor(max);
   // return Math.floor(Math.random() * (max - min + 1)) + min;}

        //console.log(getRandom(0, 19));
        //console.log(getRandom(10, 200));