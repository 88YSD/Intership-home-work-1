//1 TASK Log 3 and 6 elements from myArray to console

    //const myArray = [1, 10, 3, 6, 'ArrayElement'];
    //const start = 0;
    //const deleteCount = 2;
    //const removedItems = myArray.splice(start, deleteCount);
        //console.log(myArray);

//second method
    //const myArray = [1, 10, 3, 6, 'ArrayElement'];
    //const start = myArray.indexOf(1, 10);
    //const deleteCount = 1;
    //const removedItems = myArray.splice(start, deleteCount);
        //console.log(myArray);

//2 TASK Log type of each element

    //const myArray = [1, 10, 3, 6, 'ArrayElement'];
    //myArray.forEach(number => console.log(number))

//3 TASK Check if all elements in array is Number

    //const myArray = [1, 10, 3, 6, 'ArrayElement'];
    //const isNumber = myArray.every(function(element) {return typeof element === 'number';});
        //console.log({isNumber,});

//4 TASK Check if at least one element is bigger than 5

    //const myArray = [1, 10, 3, 6, 'ArrayElement'];
    //const isBiggerThanFive = myArray.some(value => { return value > 5 })
        //console.log({isBiggerThanFive,});

   //5 TASK NO

//6 TASK Multiply numbers of Array by 2

   //const myArray = [1, 10, 3, 6,];
   //const multiplied = myArray.myArray=myArray.map(e=>{return e*2})
       //console.log({ multiplied,});

//7 TASK Calculate array sum

    //const myArray = [1, 10, 3, 6, 'ArrayElement'];
    //const sum = myArray.reduce((total, number) => total + number, 0)
        //console.log(sum);

//8 TASK Sort array in ascending and descending order

    //const myArray = [1, 10, 3, 6,];
    //const asc = myArray.sort(function(a, b){return a-b});
    //const des = myArray.sort(function(a, b){return b-a});
        //console.log({asc, des});

    




